"""STUB — Sprint 3 implementation.

Full version builds platform-specific prompts (Kling, Veo, Sora, GPT Image,
Nano Banana, etc.) from the validated preproduction packet using per-platform
syntax templates. Sprint 1 returns a placeholder string.
"""
from __future__ import annotations

from typing import Any


def build_prompt(packet: dict[str, Any], model: str = "unset") -> str:
    """STUB: return placeholder. TODO: Sprint 3 — per-platform prompt templates."""
    return f"[STUB prompt for model={model} — Sprint 3 will build the real prompt]"
